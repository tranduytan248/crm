@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("CostType_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Add", "CostType", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "CostType_OnProcessSuccess(data, 'AddCostType')"
    }, new { @class = "form-horizontal", id = "AddCostType" }))
    {
        <div id="bodyForm">
            @Html.Partial("_CostType")
        </div>
    }
}

@section ContinueForm{

}
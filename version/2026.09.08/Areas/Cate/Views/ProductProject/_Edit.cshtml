@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("ProductProject_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Edit", "ProductProject", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "ProductProject_OnProcessSuccess(data, 'EditProductProject')"
    }, new { @class = "form-horizontal", id = "EditProductProject" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ProductProject")
        </div>
    }
}
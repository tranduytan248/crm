@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("ProductCost_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Edit", "ProductCost", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "ProductCost_OnProcessSuccess(data, 'EditProductCost')"
    }, new { @class = "form-horizontal", id = "EditProductCost" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ProductCost")
        </div>
    }
}
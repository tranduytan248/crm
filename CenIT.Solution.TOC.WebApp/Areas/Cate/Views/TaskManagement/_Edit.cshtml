@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("Task_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}
@section FormType { primary }
@section FormIcon { <i class="far fa-edit"></i> }
@section FormBody {
    @using (Ajax.BeginForm("Edit", "TaskManagement", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "TaskManagement_OnProcessSuccess(data)"
    }, new { @class = "form-horizontal", id = "EditTaskManagement", enctype = "multipart/form-data" }))
    {
        <div id="bodyForm">
            @Html.Partial("_TaskDetailForm")
        </div>
    }
}

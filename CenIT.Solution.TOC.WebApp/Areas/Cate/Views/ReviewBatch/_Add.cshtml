@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("ReviewBatch_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Add", "ReviewBatch", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "ReviewBatch_OnProcessSuccess(data, 'AddReviewBatch')"
    }, new { @class = "form-horizontal", id = "AddReviewBatch" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ReviewBatch")
        </div>
    }
}

@section ContinueForm{

}
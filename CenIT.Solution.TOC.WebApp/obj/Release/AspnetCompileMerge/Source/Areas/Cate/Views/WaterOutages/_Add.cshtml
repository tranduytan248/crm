@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("WaterOutages_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @*@using (Ajax.BeginForm("Add", "WaterOutages", new { area = "Cate" }, new AjaxOptions
        {
            HttpMethod = "POST",
            OnSuccess = "WaterOutages_OnProcessSuccess(data, 'AddWaterOutages')"
        }, new { @class = "form-horizontal", id = "AddWaterOutages" }))
        {
            <div id="bodyForm">
                @Html.Partial("_WaterOutages")
            </div>
        }*@

    @using (Html.BeginForm("Add", "WaterOutages", new { area = "Cate" }, FormMethod.Post, new { encType = "multipart/form-data", id = "AddWaterOutages", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_WaterOutages")
        </div>
    }

    <script type="text/javascript">
    $(document).ready(function () {

        $('.modal-footer #btnSave').attr('type', 'button');

        // Nhấn gửi
        $("#btnSave").click(function () {
            for (instance in CKEDITOR.instances) {
                CKEDITOR.instances[instance].updateElement();
            }
            $("form#AddWaterOutages").submit();
        });

        $('form#AddWaterOutages').ajaxForm({
            success: function (response) {
                if (response.status != undefined) {
                    if ($("#ModalContent #modal_AddWaterOutages #chkNotDismissModal").is(":checked")) {
                        if (response.status != undefined) {
                            eval(response.message);
                            _tableWaterOutages.ajax.reload(null, false);
                            response.status = undefined;
                            $("#ModalContent #modal_AddWaterOutages form")[0].reset();
                        }
                    } else {
                        if (!response.status && response.errorCode == 1) {
                            eval(response.message);
                            response.status = undefined;
                        } else {
                            $("#ModalContent #modal_AddWaterOutages").modal("hide");
                            $("#ModalContent #modal_AddWaterOutages").on('hidden.bs.modal',
                                function () {
                                    if (response.status != undefined) {
                                        eval(response.message);
                                        _tableWaterOutages.ajax.reload(null, true);
                                        response.status = undefined;
                                    }
                                });
                        }
                    }
                } else {
                    $("#ModalContent #modal_AddWaterOutages" + " #bodyForm").html(response);
                }
            }
        });
    })

    </script>

}

@section ContinueForm{

}
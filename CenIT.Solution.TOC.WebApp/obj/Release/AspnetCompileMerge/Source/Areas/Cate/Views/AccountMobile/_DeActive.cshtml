@using TSFramework.Libs.Processors
@model Core.Cate.Models.KWC_AccountMobileModel

@{
    ViewBag.Title = $"Ngưng hoạt động tài khoản - [{Model.AccountUser}]";
    Layout = "~/Views/Shared/_FormConfirm.cshtml";
}

@section FormType{
    danger
}

@section FormBody{
    @using (Ajax.BeginForm("DeActive", "AccountMobile", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "AccountMobile_OnProcessSuccess(data, 'DeActiveAccount')"
    }, new { @class = "form-horizontal", id = "DeActiveAccount" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.Account_ID)
        @Html.HiddenFor(model => model.CustomerName)
        <h5> @Html.Raw(ViewBag.ConfirmMessage)</h5>
    }
}
@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("Banners_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @*@using (Ajax.BeginForm("Add", "Banners", new { area = "Cate" }, new AjaxOptions
        {
            HttpMethod = "POST",
            OnSuccess = "Banners_OnProcessSuccess(data, 'AddBanners')"
        }, new { @class = "form-horizontal", id = "AddBanners" }))
        {
            <div id="bodyForm">
                @Html.Partial("_Banners")
            </div>
        }*@

    @using (Html.BeginForm("Add", "Banners", new { area = "Cate" }, FormMethod.Post, new { encType = "multipart/form-data", id = "AddBanners", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Banners")
        </div>
    }

    <script type="text/javascript">
    $(document).ready(function () {

        $('.modal-footer #btnSave').attr('type', 'button');

        // Nhấn gửi
        $("#btnSave").click(function () {        
            $("form#AddBanners").submit();
        });

        $('form#AddBanners').ajaxForm({
            success: function (response) {
                if (response.status != undefined) {
                    if ($("#ModalContent #modal_AddBanners #chkNotDismissModal").is(":checked")) {
                        if (response.status != undefined) {
                            eval(response.message);
                            _tableBanners.ajax.reload(null, false);
                            response.status = undefined;
                            $("#ModalContent #modal_AddBanners form")[0].reset();
                        }
                    } else {
                        if (!response.status && response.errorCode == 1) {
                            eval(response.message);
                            response.status = undefined;
                        } else {
                            $("#ModalContent #modal_AddBanners").modal("hide");
                            $("#ModalContent #modal_AddBanners").on('hidden.bs.modal',
                                function () {
                                    if (response.status != undefined) {
                                        eval(response.message);
                                        _tableBanners.ajax.reload(null, true);
                                        response.status = undefined;
                                    }
                                });
                        }
                    }
                } else {
                    $("#ModalContent #modal_AddBanners" + " #bodyForm").html(response);
                }
            }
        });
    })

    </script>

}

@section ContinueForm{

}
@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("WaterOutages_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Html.BeginForm("Edit", "WaterOutages", new { area = "Cate" }, FormMethod.Post, new { encType = "multipart/form-data", id = "EditWaterOutages", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_WaterOutages")
        </div>
    }

    <script type="text/javascript">
    $(document).ready(function () {

            $('.modal-footer #btnSave').attr('type', 'button');

            // Nhấn gửi
            $("#btnSave").click(function () {
                for (instance in CKEDITOR.instances) {
                    CKEDITOR.instances[instance].updateElement();
                }
                $("form#EditWaterOutages").submit();
            });

            $('form#EditWaterOutages').ajaxForm({
                success: function (response) {
                    if (response.status != undefined) {
                        if ($("#ModalContent #modal_EditWaterOutages #chkNotDismissModal").is(":checked")) {
                            if (response.status != undefined) {
                                eval(response.message);
                                _tableWaterOutages.ajax.reload(null, false);
                                response.status = undefined;
                                $("#ModalContent #modal_EditWaterOutages form")[0].reset();
                            }
                        } else {
                            if (!response.status && response.errorCode == 1) {
                                eval(response.message);
                                response.status = undefined;
                            } else {
                                $("#ModalContent #modal_EditWaterOutages").modal("hide");
                                $("#ModalContent #modal_EditWaterOutages").on('hidden.bs.modal',
                                    function () {
                                        if (response.status != undefined) {
                                            eval(response.message);
                                            _tableWaterOutages.ajax.reload(null, true);
                                            response.status = undefined;
                                        }
                                    });
                            }
                        }
                    } else {
                        $("#ModalContent #modal_EditWaterOutages" + " #bodyForm").html(response);
                    }
                }
            });
    })
    </script>

    @*@using (Ajax.BeginForm("Edit", "WaterOutages", new { area = "Cate" }, new AjaxOptions
        {
            HttpMethod = "POST",
            OnSuccess = "WaterOutages_OnProcessSuccess(data, 'EditWaterOutages')"
        }, new { @class = "form-horizontal", id = "EditWaterOutages" }))
        {
            <div id="bodyForm">
                @Html.Partial("_WaterOutages")
            </div>
        }*@
}

@section ContinueForm{

}
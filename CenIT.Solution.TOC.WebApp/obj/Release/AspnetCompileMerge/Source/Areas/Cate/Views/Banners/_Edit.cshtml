@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("Banners_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Html.BeginForm("Edit", "Banners", new { area = "Cate" }, FormMethod.Post, new { encType = "multipart/form-data", id = "EditBanners", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Banners")
        </div>
    }

    <script type="text/javascript">
    $(document).ready(function () {

            $('.modal-footer #btnSave').attr('type', 'button');

            // Nhấn gửi
            $("#btnSave").click(function () {              
                $("form#EditBanners").submit();
            });

            $('form#EditBanners').ajaxForm({
                success: function (response) {
                    if (response.status != undefined) {
                        if ($("#ModalContent #modal_EditBanners #chkNotDismissModal").is(":checked")) {
                            if (response.status != undefined) {
                                eval(response.message);
                                _tableBanners.ajax.reload(null, false);
                                response.status = undefined;
                                $("#ModalContent #modal_EditBanners form")[0].reset();
                            }
                        } else {
                            if (!response.status && response.errorCode == 1) {
                                eval(response.message);
                                response.status = undefined;
                            } else {
                                $("#ModalContent #modal_EditBanners").modal("hide");
                                $("#ModalContent #modal_EditBanners").on('hidden.bs.modal',
                                    function () {
                                        if (response.status != undefined) {
                                            eval(response.message);
                                            _tableBanners.ajax.reload(null, true);
                                            response.status = undefined;
                                        }
                                    });
                            }
                        }
                    } else {
                        $("#ModalContent #modal_EditBanners" + " #bodyForm").html(response);
                    }
                }
            });
    })
    </script>

    @*@using (Ajax.BeginForm("Edit", "Banners", new { area = "Cate" }, new AjaxOptions
        {
            HttpMethod = "POST",
            OnSuccess = "Banners_OnProcessSuccess(data, 'EditBanners')"
        }, new { @class = "form-horizontal", id = "EditBanners" }))
        {
            <div id="bodyForm">
                @Html.Partial("_Banners")
            </div>
        }*@
}

@section ContinueForm{

}
@using TSFramework.Libs.Processors
@model Core.Cate.Models.KWC_NotificationDeviceModel

@{
    ViewBag.Title = "Gửi thông báo tiền cước cho tất cả các hợp đồng";
    Layout = "~/Views/Shared/_FormConfirm.cshtml";
}

@section FormType{
    warning
}

@section FormBody{
    @using (Ajax.BeginForm("SendNotificationAllDevice", "Notification", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "Notification_OnProcessSuccess(data, 'SendNotificationAllDevice')"
    }, new { @class = "form-horizontal", id = "SendNotificationAllDevice" }))
    {
        @Html.AntiForgeryToken()   
        @Html.HiddenFor(model => model.Period)
        <h5> @Html.Raw(ViewBag.ConfirmMessage)</h5>
    }
}
@model Core.Sys.Models.Sys.SysUserModel

@{
    ViewBag.Title = $"Reset mật khẩu cho tài khoản - [{Model.FullName} - {Model.UserName}]";
    Layout = "~/Views/Shared/_FormConfirm.cshtml";
}

@section FormType{
    warning
}

@section FormBody{

    @using (Ajax.BeginForm("ResetPassword", "User", new { area = "Sys" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "User_OnProcessSuccess(data, 'ResetPassword')"
    }, new { @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.UserId)
        @Html.HiddenFor(model => model.UserName)
        @Html.HiddenFor(model => model.FullName)
        @Html.HiddenFor(model => model.Email)
        @Html.HiddenFor(model => model.Reason)

        <h5> @Html.Raw(ViewBag.ConfirmMessage)</h5>
        <hr/>
    }
}
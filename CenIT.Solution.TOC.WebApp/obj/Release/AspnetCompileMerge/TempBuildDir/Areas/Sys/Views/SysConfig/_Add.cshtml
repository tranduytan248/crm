@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("SysConfig_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

	@*@using (Ajax.BeginForm("Add", "SysConfig", new { area = "Sys" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "AppSetting_OnProcessSuccess(data, 'AddConfig')"
    }, new { @class = "form-horizontal" }))*@
	@using (Html.BeginForm("Add", "SysConfig", new { area = "Sys" }, FormMethod.Post, new { encType = "multipart/form-data", id = "AddConfig", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_SysConfig")
        </div>
    }
    
	<script type="text/javascript">
        $('form#AddConfig').ajaxForm({
            success: function (response) {
                if (response.status != undefined) {
                    if ($("#ModalContent #modal_AddConfig #chkNotDismissModal").is(":checked")) {
                        if (response.status != undefined) {
                            eval(response.message);
                            _tableAppSetting.ajax.reload(null, false);
                            response.status = undefined;
                            $("#ModalContent #modal_AddConfig form")[0].reset();
                        }
                    } else {
                        $("#ModalContent #modal_AddConfig").modal("hide");
                        $("#ModalContent #modal_AddConfig").on('hidden.bs.modal',
                            function () {
                                if (response.status != undefined) {
                                    eval(response.message);
                                    _tableAppSetting.ajax.reload(null, false);
                                    response.status = undefined;
                                }
                            });
                    }
                } else {
                    $("#ModalContent #modal_AddConfig" + " #bodyForm").html(response);
                }
            }
        });
    </script>
}

@section ContinueForm{

}
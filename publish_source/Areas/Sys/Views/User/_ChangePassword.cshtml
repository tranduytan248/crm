@using EnumHelper = TSFramework.Libs.Helpers.EnumHelper
@{
    ViewBag.Title = "Thay đổi mật khẩu";
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{@Html.Raw(EnumHelper.GetDescription(EnumTypeModal.Primary))}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{
    @using (Ajax.BeginForm("ChangePassword", "User", new { area = "Sys" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "User_OnProcessSuccess(data, 'ChangePassword')"
    }, new { @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Password")
        </div>
    }

    <script src="@Url.Content("~/Areas/Sys/Views/User/PasswordValid.js")"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            var confirmPassValid = "#ConfirmPassword";
            $("#NewPassword").passwordValidation({ "confirmField": confirmPassValid, "minLength": 8 },
                function(element, valid, match, failedCases) {
                    $("span[data-valmsg-for='NewPassword']").empty();
                    $.each(failedCases,
                        function(idx, msg) {
                            $("span[data-valmsg-for='NewPassword']").append(' <p class="text-red">' + msg + "</p>");
                        });
                    if (valid) $(element).css("border", "1px solid #00a65a");
                    if (!valid) $(element).css("border", "1px solid #dd4b39");
                    if (valid && match) $(confirmPassValid).css("border", "1px solid #00a65a");
                    if (!valid || !match) $(confirmPassValid).css("border", "1px solid #dd4b39");
                });
        });
    </script>
}

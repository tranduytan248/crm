@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("RM_ExchangeHistory_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Edit", "SalesTeamMembers", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "BusinessOpportunity_OnProcessSuccess(data, 'EditExchangeHistory')"
    }, new { @class = "form-horizontal", id = "EditExchangeHistory" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ExchangeHistory")
        </div>
    }
}
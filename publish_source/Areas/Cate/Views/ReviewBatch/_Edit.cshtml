@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("ReviewBatch_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="far fa-edit"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Edit", "ReviewBatch", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "ReviewBatch_OnProcessSuccess(data, 'EditReviewBatch')"
    }, new { @class = "form-horizontal", id = "EditReviewBatch" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ReviewBatch")
        </div>
    }
}
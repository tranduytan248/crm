@using TSFramework.Libs.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("EmployeeBusinessPlan_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Add", "EmployeeBusinessPlan", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "EmployeeBusinessPlan_OnProcessSuccess(data, 'AddEmployeeBusinessPlan')"
    }, new { @class = "form-horizontal", id = "AddEmployeeBusinessPlan" }))
    {
        <div id="bodyForm">
            @Html.Partial("_EmployeeBusinessPlan")
        </div>
    }
}

@section ContinueForm{

}